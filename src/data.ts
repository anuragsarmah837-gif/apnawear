import { Product } from './types';

// Static products catalog cleared for database connection
export const INITIAL_PRODUCTS: Product[] = [];

// Static daily deals catalog cleared for database connection
export const DAILY_DEALS: Product[] = [];
